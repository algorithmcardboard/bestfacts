(function(angular, app) {
  "use strict";

  app.controller("RegistrationController",["$scope", "$window", function($scope, $window) {
    console.log('inside registration controller');

    $scope.registerUser = function(){
    };
  }]);
})(angular, app);

